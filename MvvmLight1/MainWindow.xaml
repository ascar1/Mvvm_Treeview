<Window x:Class="MvvmLight1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        Height="376"
        Width="792.857"
        Title="MVVM Light Application"
        DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="LayoutRoot">


        <TextBlock FontSize="10"
                   FontWeight="Bold"
                   Foreground="Purple"
                   Text="{Binding WelcomeTitle}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap" Margin="270,10,349,306" Width="166" Height="30" RenderTransformOrigin="0.512,0.5" />
        <TreeView ItemsSource="{Binding LavelList}" HorizontalAlignment="Left" Height="256" Margin="10,49,0,0" VerticalAlignment="Top" Width="138">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel>
                        <TextBlock x:Name="textBlock" Text="{Binding name}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>

            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded}"/>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
        <Button Command="{Binding MyCommand}" Content="Button" HorizontalAlignment="Left" Margin="10,310,0,0" VerticalAlignment="Top" Width="75"/>

        <DataGrid ItemsSource="{Binding Path=ParamList}" HorizontalAlignment="Left" Height="256" Margin="153,49,0,0" VerticalAlignment="Top" Width="622" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Path=name}" Width="90" />
                <DataGridTextColumn Header="Тип" Binding="{Binding Path=type}" Width="80" />
                <DataGridTextColumn Header="Значение" Binding="{Binding Path=val}" Width="100" />
                <DataGridTextColumn Header="Коментарий" Binding="{Binding Path=comment}" Width="300" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
