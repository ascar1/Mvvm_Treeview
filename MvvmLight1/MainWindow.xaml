<Window x:Class="MvvmLight1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ignore="http://www.galasoft.ch/ignore"
        mc:Ignorable="d ignore"
        Height="316.071"
        Width="792.857"
        Title="MVVM Light Application"
        DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Skins/MainSkin.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid x:Name="LayoutRoot">
        

        <TextBlock FontSize="10"
                   FontWeight="Bold"
                   Foreground="Purple"
                   Text="{Binding WelcomeTitle}"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap" Margin="98,10,147,259" Width="166" Height="17" RenderTransformOrigin="0.5,0.5" />
        <TreeView ItemsSource="{Binding LavelList}" HorizontalAlignment="Left" Height="198" Margin="10,49,0,0" VerticalAlignment="Top" Width="138">
            <!--
            <i:Interaction.Behaviors>
                <utils:BindableSelectedItemBehavior SelectedItem="{Binding SelectedCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </i:Interaction.Behaviors>
            -->
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel>
                        <TextBlock x:Name="textBlock" Text="{Binding name}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
 
        <ListView ItemsSource="{Binding Path=ParamList}" HorizontalAlignment="Left" Height="198" Margin="153,49,0,0" VerticalAlignment="Top" Width="622">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="100" Header="Name" DisplayMemberBinding="{Binding name}"/>
                    <GridViewColumn Width="100" Header="Type" DisplayMemberBinding="{Binding type }"/>
                    <GridViewColumn Width="100" Header="Val" DisplayMemberBinding="{Binding val}"/>
                    <GridViewColumn Width="300" Header="Comment" DisplayMemberBinding="{Binding comment}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
